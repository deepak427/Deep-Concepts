# Types

TypeScript type definitions for the application.

## Type Files

- `learning.ts` - Generic learning system types (achievements, quiz results, review items, etc.)
- `quantum.ts` - Quantum computing-specific types (qubit states, gates, circuits, etc.)
